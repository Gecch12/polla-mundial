nav('charts.html');function bars(el,rows,label,value,colorFn){const max=Math.max(...rows.map(value),1);document.getElementById(el).innerHTML=rows.map(r=>`<div class="barrow"><div class="barlabel">${esc(label(r))}</div><div><div class="bar" style="width:${Math.max(2,value(r)/max*100)}%;background:${colorFn?colorFn(r):'linear-gradient(90deg,#f5c542,#fff2a6)'}"></div></div><b>${value(r)}</b></div>`).join('')}bars('leaders',leaders.slice().sort((a,b)=>b.Dias_Lider-a.Dias_Lider),r=>r.Participante,r=>r.Dias_Lider);bars('gap',latestRows().slice(0,10),r=>r.Participante,r=>Math.max(0,r.Distancia_Lider),r=>`linear-gradient(90deg,${colorForName(r.Participante)},#ffffff)`);